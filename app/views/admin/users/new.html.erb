<div class="admin-page-header">
  <%= link_to admin_users_path, class: "back-link" do %>
    <svg width="16" height="16" fill="currentColor" viewBox="0 0 16 16" style="margin-right: 4px;">
      <path fill-rule="evenodd" d="M15 8a.5.5 0 0 0-.5-.5H2.707l3.147-3.146a.5.5 0 1 0-.708-.708l-4 4a.5.5 0 0 0 0 .708l4 4a.5.5 0 0 0 .708-.708L2.707 8.5H14.5A.5.5 0 0 0 15 8z"/>
    </svg>
    Retour aux utilisateurs
  <% end %>
  <h1>Créer un nouvel utilisateur</h1>
  <p class="text-muted">Ajoutez manuellement un nouveau compte artisan</p>
</div>

<div class="admin-card">
  <%= form_with url: admin_users_path, method: :post, local: true do |f| %>
    <!-- Personal Information Section -->
    <div style="margin-bottom: var(--spacing-xl);">
      <h3 style="margin-bottom: var(--spacing-lg); color: var(--primary-green);">Informations Personnelles</h3>
      
      <div class="form-fields-grid">
        <div class="form-group">
          <%= f.label :first_name, "Prénom", class: "form-label" %>
          <%= f.text_field :first_name, class: "form-input", placeholder: "Jean", required: true %>
        </div>

        <div class="form-group">
          <%= f.label :last_name, "Nom", class: "form-label" %>
          <%= f.text_field :last_name, class: "form-input", placeholder: "Dupont", required: true %>
        </div>
      </div>

      <div class="form-fields-grid">
        <div class="form-group">
          <%= f.label :email, "Email", class: "form-label" %>
          <%= f.email_field :email, class: "form-input", placeholder: "jean.dupont@example.com", required: true %>
          <span class="form-hint">L'adresse email pour la connexion au compte</span>
        </div>

        <div class="form-group">
          <%= f.label :whatsapp_phone, "Téléphone WhatsApp", class: "form-label" %>
          <%= f.text_field :whatsapp_phone, class: "form-input", placeholder: "+33612345678", required: true %>
          <span class="form-hint">Format: +33612345678 (sans espaces)</span>
        </div>
      </div>

      <div class="form-group">
        <%= f.label :preferred_language, "Langue préférée", class: "form-label" %>
        <%= f.select :preferred_language, 
          [["🇫🇷 Français", "fr"], ["🇹🇷 Türkçe", "tr"]], 
          {}, 
          class: "form-select", required: true %>
      </div>
    </div>

    <!-- Business Information Section -->
    <div style="margin-bottom: var(--spacing-xl);">
      <h3 style="margin-bottom: var(--spacing-lg); color: var(--primary-green);">Informations Professionnelles</h3>
      
      <div class="form-group">
        <%= f.label :company_name, "Nom de l'entreprise", class: "form-label" %>
        <%= f.text_field :company_name, class: "form-input", placeholder: "Dupont Maçonnerie SARL", required: true %>
      </div>

      <div class="form-fields-grid">
        <div class="form-group">
          <%= f.label :siret, "Numéro SIRET", class: "form-label" %>
          <%= f.text_field :siret, class: "form-input", placeholder: "12345678900012", required: true %>
          <span class="form-hint">14 chiffres sans espaces</span>
        </div>

        <div class="form-group">
          <%= f.label :vat_number, "Numéro de TVA", class: "form-label" %>
          <%= f.text_field :vat_number, class: "form-input", placeholder: "FR12345678901" %>
          <span class="form-hint">Optionnel - Format: FR + 11 chiffres</span>
        </div>
      </div>

      <div class="form-group">
        <%= f.label :address, "Adresse complète", class: "form-label" %>
        <%= f.text_area :address, class: "form-textarea", rows: 3, placeholder: "15 Rue de la Construction\n75001 Paris\nFrance", required: true %>
      </div>
    </div>

    <!-- Account Settings Section -->
    <div style="margin-bottom: var(--spacing-xl);">
      <h3 style="margin-bottom: var(--spacing-lg); color: var(--primary-green);">Paramètres du Compte</h3>
      
      <div class="form-fields-grid">
        <div class="form-group">
          <%= f.label :password, "Mot de passe temporaire", class: "form-label" %>
          <%= f.password_field :password, class: "form-input", placeholder: "Minimum 6 caractères", required: true %>
          <span class="form-hint">L'utilisateur pourra le changer après connexion</span>
        </div>

        <div class="form-group">
          <%= f.label :password_confirmation, "Confirmation du mot de passe", class: "form-label" %>
          <%= f.password_field :password_confirmation, class: "form-input", placeholder: "Retapez le mot de passe", required: true %>
        </div>
      </div>

      <div class="form-group">
        <%= f.label :account_status, "Statut du compte", class: "form-label" %>
        <%= f.select :account_status, 
          [
            ["En attente (Pending)", "pending"],
            ["Actif (Active)", "active"],
            ["Suspendu (Suspended)", "suspended"]
          ], 
          {}, 
          class: "form-select", required: true %>
        <span class="form-hint">Sélectionnez "Actif" si l'utilisateur a déjà payé</span>
      </div>
    </div>

    <!-- Stripe Information Section (Optional) -->
    <div style="margin-bottom: var(--spacing-xl);">
      <h3 style="margin-bottom: var(--spacing-lg); color: var(--primary-green);">Informations Stripe (Optionnel)</h3>
      
      <div class="form-group">
        <%= f.label :stripe_customer_id, "ID Client Stripe", class: "form-label" %>
        <%= f.text_field :stripe_customer_id, class: "form-input", placeholder: "cus_xxxxxxxxxxxxx" %>
        <span class="form-hint">Laissez vide si pas encore configuré - sera ajouté automatiquement lors du paiement</span>
      </div>
    </div>

    <!-- Form Actions -->
    <div class="form-actions">
      <%= f.submit "Créer l'utilisateur", class: "btn btn-primary" %>
      <%= link_to "Annuler", admin_users_path, class: "btn btn-secondary" %>
    </div>

    <div class="alert alert-info" style="margin-top: var(--spacing-lg);">
      <strong>Note:</strong> Un email de bienvenue sera envoyé automatiquement à l'utilisateur avec ses identifiants de connexion. Assurez-vous que l'adresse email est correcte.
    </div>
  <% end %>
</div>
